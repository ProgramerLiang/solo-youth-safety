# Backend 已移除

当前项目状态、能力边界、路线图与任务清单统一维护在 `../README.md`。

后端 FastAPI + SQLite 代码已从仓库移除；当前不再作为运行、验证、联调、路线图或验收目标。`backend/` 仅保留本说明和本地数据库文件 `data/safety.db`。

`data/safety.db` 是本地数据库文件，不代表当前仍有后端运行链路；不要在未明确告知用户前删除或重建它。

如需了解当前可承诺能力，请查看 `../README.md`。除非重新立项并先写新契约，否则不要恢复后端开发任务。
